name: dependabot auto-merge

on: pull_request

permissions:
  contents: read

jobs:
  auto-merge:
    # The called workflow's token can hold no more than this grants.
    permissions:
      contents: write
      pull-requests: write
    uses: lolaf-org/workflows/.github/workflows/dependabot-auto-merge.yml@v1.0.0
